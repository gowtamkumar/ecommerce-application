import React from "react";
import ProductCardDetails from "../product/productcard/ProductCardDetails";
import { getWishlists } from "@/lib/apis/wishlist";

export default async function MyWishlist({ user }: any) {
  console.log("🚀 ~ wishlist:", user);
  // const wishlists = await getWishlists();
  // console.log("🚀 ~ wishlists:", wishlists);
  return (
    // <ProductCardDetails />
    <div>asdkfjasdf</div>
  );
}
