name: first workflow

on: [push]

jobs:
  first-workflow:
    runs-on: ubontu-latest

    steps:
      - name: First step
        run: |
          echo "learn git action"
          echo "hello world"
