name: first workflow

on: [push]

jobs:
  first-workflow:
    runs-on: ubuntu-latest

    steps:
      - name: First step
        run: |
          echo "learn git action"
          echo "hello world"
