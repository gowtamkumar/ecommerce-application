### Stage 1 ###
FROM node:22.16.0-alpine

WORKDIR /app

COPY package.json ./

RUN npm install --legacy-peer-deps
# RUN npm run build

COPY . .

ARG DEFAULT_PORT=3900
ENV PORT=$DEFAULT_PORT

EXPOSE $PORT

CMD [ "npm", "run", "dev" ]